# 数字四则运算，打印结果均为8
print(5+3)
print(10-2)
print(2*4)
print(int(16/2))

# int()函数将浮点数转化为整数
# int()

# floor()函数与int()函数类似，返回接近原数但小于原数的整型。
# （返回数值一定小于原数值，尤其注意负数，与int()有区别）。
# floor()函数不能直接使用，需要导入库（from math import *）。
# from math import *
# floor()

# round()函数：四舍五入，得到最接近原数的整型。
# （注意奇进偶不进 xxx.5）
# round()
